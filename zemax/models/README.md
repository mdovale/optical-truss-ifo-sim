# Zemax models

Place the OTI input-stage OpticStudio prescription here, for example:

- `oti_input_stage.zmx` — nominal compact input stage referenced by `configs/nominal.yaml`

ZOSPy integration is implemented in Milestone 3 (`optical_truss_ifo_sim.zemax_api`,
`beam_export`).
